spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        default-label: master
        git:
          uri: https://github.com/MelisaDe/config-server-t1
server:
  port: 8888